apiVersion: v1
kind: Service
metadata:
  name: {{ include "bedrock-gateway.fullname" . }}
  labels:
    {{- include "bedrock-gateway.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bedrock-gateway.selectorLabels" . | nindent 4 }}
